# Local values only. Never commit actual credentials.
# Optional public contact address. Leave blank until approved for publication.
# PUBLIC_CONTACT_EMAIL=
# Cloudflare Web Analytics site token. Optional; omitted builds emit no beacon.
# PUBLIC_CF_BEACON_TOKEN=
# TRAKT_CLIENT_ID=
# TRAKT_REFRESH_TOKEN=
